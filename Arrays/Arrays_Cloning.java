// package Arrays;

import java.lang.reflect.Array;
import java.util.Arrays;

class Arrays_cloning {

    static void printArray(int array[]) {
        for (int i = 0; i < array.length; i++) {
            System.out.print(array[i] + "\t");
        }
    }
    // Shallow Copy
    // Shallow Copy

    // static void changeArray(int array[]) {
    // array[0] = 100;
    // array[array.length - 1] = 10;
    // }

    public static void main(String[] args) {

        // Shallow Copy
        // Shallow Copy
        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;

        // changeArray(array);
        // printArray(array);

        //
        //
        //
        //

        // Deep copy
        // Deep copy

        // copyOf clone()
        // copyOf clone()

        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;
        // printArray(array);

        // System.out.println();

        // int newArray[] = array.clone();
        // newArray[0] = 0;
        // newArray[1] = 0;
        // printArray(newArray);

        // copyOf Method()
        // copyOf Method()

        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;
        // printArray(array);

        // System.out.println();

        // int newArray[] = Arrays.copyOf(array, array.length);
        // newArray[0] = 0;
        // newArray[1] = 0;
        // printArray(newArray);

        // copyOf Method()
        // copyOf Method()

        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;
        // printArray(array);

        // System.out.println();

        // int newArray[] = Arrays.copyOf(array, array.length);
        // newArray[0] = 0;
        // newArray[1] = 0;
        // printArray(newArray);

        // copyOfRange Method()
        // copyOfRange Method()

        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;
        // printArray(array);

        // System.out.println();

        // int newArray[] = Arrays.copyOfRange(array, 0, array.length);
        // newArray[0] = 0;
        // newArray[1] = 0;
        // printArray(newArray);

        // Range of indexes using copyOfRange Method
        // Range of indexes using copyOfRange Method

        // int array[] = new int[5];
        // array[0] = 1;
        // array[1] = 2;
        // array[2] = 3;
        // array[3] = 4;
        // array[4] = 5;
        // printArray(array);

        // System.out.println();

        // int newArray[] = Arrays.copyOfRange(array, 0, 3);
        // newArray[0] = 0;
        // newArray[1] = 0;
        // printArray(newArray);

    }
}
